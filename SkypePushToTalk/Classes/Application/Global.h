
// copied from Carbon to avoid inclusion (prefix changed to avoid collision)
enum {
	KeyF1 = 0x7A,
};

#define kKeyComboPreferenceKey @"KeyCombo"
#define kFirstRunHappenedPreferenceKey @"FirstRunHappened"
